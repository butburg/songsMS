<persistence version="2.1"
             xmlns="http://xmlns.jcp.org/xml/ns/persistence" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd">

    <persistence-unit name="songDB-PU" transaction-type="RESOURCE_LOCAL">

        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>model.Song</class>
        <class>model.SongList</class>
        <class>model.User</class>
        <exclude-unlisted-classes>true</exclude-unlisted-classes>

        <properties>
            <property name="javax.persistence.jdbc.driver" value="com.mysql.jdbc.Driver"/>
            <property name="javax.persistence.jdbc.url" value="jdbc:mysql://db4free.net:3306/sevenfour_songs" />
            <property name="javax.persistence.jdbc.user" value="sevenfour" />
            <property name="javax.persistence.jdbc.password" value="Saal_Wiese" />

            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL8Dialect"/>
            <property name="hibernate.show_sql" value="true" />
            <property name="hibernate.format_sql" value="false"/>

            <property name="spring.jpa.hibernate.ddl-auto" value="create-drop"/>
            <property name="hibernate.hbm2ddl.auto" value="create"/>
            <property name="spring.datasource.initialization-mode" value="always"/>

            <!-- The connection is released after each statement execution
                 and reacquired prior to running the next statement. -->
            <property name="hibernate.connection.release_mode" value="after_statement"></property>

            <!-- Connection pool properties -->
            <property name="hibernate.c3p0.min_size" value="1"/>
            <property name="hibernate.c3p0.max_size" value="2"/>

            <!-- in seconds:  When an idle connection is removed from the pool
                 Hibernate default: 0, never expire. -->
            <property name="hibernate.c3p0.timeout" value="180"/>

            <!--  Number of prepared statements that will be cached.
                  Hibernate default: 0 , caching is disable. -->
            <property name="hibernate.c3p0.max_statements" value="50"/>

        </properties>
    </persistence-unit>

</persistence>